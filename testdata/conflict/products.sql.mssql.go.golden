// Code generated by sqlgen; DO NOT EDIT.
package conflict

import (
	"context"
	"database/sql"
)

const upsertProductConstMSSQL = `MERGE INTO products AS target
USING (VALUES (@p1, @p2, @p3, 1)) AS src (sku, name, price, stock)
ON target.sku = src.sku
WHEN NOT MATCHED THEN INSERT (sku, name, price, stock) VALUES (src.sku, src.name, src.price, src.stock)
WHEN MATCHED THEN UPDATE SET name = src.name, price = src.price;`

type upsertProductMSSQL struct {
	stmt *sql.Stmt
	db   *sql.DB
}

func (r *upsertProductMSSQL) exec(ctx context.Context, sku string, name string, price float64) (sql.Result, error) {
	if r.stmt == nil { var err error; r.stmt, err = r.db.PrepareContext(ctx, upsertProductConstMSSQL); if err != nil { return nil, err } }
	return r.stmt.ExecContext(ctx, sku, name, price)
}

func (r *upsertProductMSSQL) close() error { if r.stmt != nil { return r.stmt.Close() }; return nil }
func (r *upsertProductMSSQL) withTx(tx *sql.Tx) upsertProductRunner { return &upsertProductMSSQL{stmt: tx.Stmt(r.stmt)} }

const upsertProductRCConstMSSQL = `MERGE INTO products AS target
USING (VALUES (@p1, @p2, @p3, 1)) AS src (sku, name, price, stock)
ON target.sku = src.sku
WHEN NOT MATCHED THEN INSERT (sku, name, price, stock) VALUES (src.sku, src.name, src.price, src.stock)
OUTPUT INSERTED.id;`

type upsertProductRCMSSQL struct {
	stmt *sql.Stmt
	db   *sql.DB
}

func (r *upsertProductRCMSSQL) execReturning(ctx context.Context, sku string, name string, price float64) (int64, error) {
	var id int64
	if r.stmt == nil { var err error; r.stmt, err = r.db.PrepareContext(ctx, upsertProductRCConstMSSQL); if err != nil { return 0, err } }
	row := r.stmt.QueryRowContext(ctx, sku, name, price)
	if err := row.Scan(&id); err != nil { return 0, err }
	return id, nil
}

func (r *upsertProductRCMSSQL) close() error { if r.stmt != nil { return r.stmt.Close() }; return nil }
func (r *upsertProductRCMSSQL) withTx(tx *sql.Tx) upsertProductRCRunner { return &upsertProductRCMSSQL{stmt: tx.Stmt(r.stmt)} }


type productsRunnerFactoryMSSQL struct {}


func (f *productsRunnerFactoryMSSQL) newUpsertProduct(db *sql.DB) upsertProductRunner { return &upsertProductMSSQL{db: db} }

func (f *productsRunnerFactoryMSSQL) newUpsertProductRC(db *sql.DB) upsertProductRCRunner { return &upsertProductRCMSSQL{db: db} }


