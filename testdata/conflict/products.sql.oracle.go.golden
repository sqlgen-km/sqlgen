// Code generated by sqlgen; DO NOT EDIT.
package conflict

import (
	"context"
	"database/sql"
)

const upsertProductConstOracle = `MERGE INTO products t
USING (SELECT :1 AS sku, :2 AS name, :3 AS price, 1 AS stock FROM dual) s
ON (t.sku = s.sku)
WHEN NOT MATCHED THEN
    INSERT (sku, name, price, stock) VALUES (s.sku, s.name, s.price, s.stock)
WHEN MATCHED THEN
    UPDATE SET name = s.name, price = s.price`

type upsertProductOracle struct {
	stmt *sql.Stmt
	db   *sql.DB
}

func (r *upsertProductOracle) exec(ctx context.Context, sku string, name string, price float64) (sql.Result, error) {
	if r.stmt == nil { var err error; r.stmt, err = r.db.PrepareContext(ctx, upsertProductConstOracle); if err != nil { return nil, err } }
	return r.stmt.ExecContext(ctx, sku, name, price)
}

func (r *upsertProductOracle) close() error { if r.stmt != nil { return r.stmt.Close() }; return nil }
func (r *upsertProductOracle) withTx(tx *sql.Tx) upsertProductRunner { return &upsertProductOracle{stmt: tx.Stmt(r.stmt)} }

const upsertProductRCConstOracle = `BEGIN
  INSERT INTO products (sku, name, price, stock) VALUES (:1, :2, :3, 1) RETURNING id INTO :out0;
EXCEPTION WHEN DUP_VAL_ON_INDEX THEN NULL;
END;`

type upsertProductRCOracle struct {
	stmt *sql.Stmt
	db   *sql.DB
}

func (r *upsertProductRCOracle) execReturning(ctx context.Context, sku string, name string, price float64) (int64, error) {
	var id int64
	if r.stmt == nil { var err error; r.stmt, err = r.db.PrepareContext(ctx, upsertProductRCConstOracle); if err != nil { return 0, err } }
	if _, err := r.stmt.ExecContext(ctx, sku, name, price, &id); err != nil { return 0, err }
	return id, nil
}

func (r *upsertProductRCOracle) close() error { if r.stmt != nil { return r.stmt.Close() }; return nil }
func (r *upsertProductRCOracle) withTx(tx *sql.Tx) upsertProductRCRunner { return &upsertProductRCOracle{stmt: tx.Stmt(r.stmt)} }


type productsRunnerFactoryOracle struct {}


func (f *productsRunnerFactoryOracle) newUpsertProduct(db *sql.DB) upsertProductRunner { return &upsertProductOracle{db: db} }

func (f *productsRunnerFactoryOracle) newUpsertProductRC(db *sql.DB) upsertProductRCRunner { return &upsertProductRCOracle{db: db} }


