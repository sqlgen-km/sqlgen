// Code generated by sqlgen; DO NOT EDIT.
package com.example.mapper;

import org.apache.ibatis.session.SqlSession;

/** Factory for creating {@code ProductsMapper} instances. */
public class ProductsMapperFactory {

    /**
     * Create a {@code ProductsMapper} for the given database driver.
     * <p>
     * Usage without Spring:
     * <pre>
     * SqlSession session = sqlSessionFactory.openSession();
     * ProductsMapper mapper = ProductsMapperFactory.create(session, "postgresql");
     * </pre>
     *
     * Valid driver names: postgresql, mysql.
     */
    public static ProductsMapper create(SqlSession session, String driver) {
        switch (driver) {
            case "postgresql" -> { return session.getMapper(ProductsMapperPG.class); }
            case "mysql" -> { return session.getMapper(ProductsMapperMySQL.class); }
        }
        throw new IllegalArgumentException(
            "Unsupported driver: " + driver + ". Supported: postgresql, mysql");
    }
}
