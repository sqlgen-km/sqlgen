// Code generated by sqlgen; DO NOT EDIT.
package com.example.mapper;

import com.example.entity.Item;

import java.util.List;
import java.math.BigDecimal;

import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.*;
import org.springframework.context.annotation.Profile;

@Mapper
@Profile("pg")
public interface ItemsMapperPG extends ItemsMapper {

    @Override
    @Select("SELECT id, name, price, stock FROM items WHERE id = #{id}")
    Item findByID(@Param("id") long id);

    @Override
    @Select("SELECT id, name, price, stock FROM items ORDER BY id")
    List<Item> findAll();

    @Override
    @Select("SELECT count(*) FROM items")
    long countItems();

    @Override
    @Insert("INSERT INTO items (name, price, stock) VALUES (#{name}, #{price}, #{stock}) RETURNING id")
    @Options(useGeneratedKeys = true, keyProperty = "id", keyColumn = "id")
    long insertItem(Item item);

    @Override
    @Update("UPDATE items SET name = #{name}, price = #{price} WHERE id = #{id}")
    long updateItem(@Param("name") String name, @Param("price") java.math.BigDecimal price, @Param("id") long id);

    @Override
    @Delete("DELETE FROM items WHERE id = #{id}")
    void deleteItem(@Param("id") long id);

}
