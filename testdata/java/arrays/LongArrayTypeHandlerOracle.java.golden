// Code generated by sqlgen; DO NOT EDIT.
package com.example.mapper.arr;

import org.apache.ibatis.type.BaseTypeHandler;
import org.apache.ibatis.type.JdbcType;

import java.sql.CallableStatement;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;

/** TypeHandler mapping {@code long[]} to a ORACLE array/JSON value. */
public class LongArrayTypeHandlerOracle extends BaseTypeHandler<long[]> {

    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, long[] parameter, JdbcType jdbcType) throws SQLException {
        Object[] boxed = new Object[parameter.length];
        for (int j = 0; j < parameter.length; j++) {
            boxed[j] = parameter[j];
        }
        oracle.jdbc.OracleConnection conn = ps.getConnection().unwrap(oracle.jdbc.OracleConnection.class);
        ps.setArray(i, conn.createARRAY("SYS.ODCINUMBERLIST", boxed));
    }

    @Override
    public long[] getNullableResult(ResultSet rs, String columnName) throws SQLException {
        return toArray(rs.getArray(columnName));
    }

    @Override
    public long[] getNullableResult(ResultSet rs, int columnIndex) throws SQLException {
        return toArray(rs.getArray(columnIndex));
    }

    @Override
    public long[] getNullableResult(CallableStatement cs, int columnIndex) throws SQLException {
        return toArray(cs.getArray(columnIndex));
    }

    private long[] toArray(java.sql.Array array) throws SQLException {
        if (array == null) {
            return null;
        }
        Object raw = array.getArray();
        Object[] items = (Object[]) raw;
        long[] out = new long[items.length];
        for (int j = 0; j < items.length; j++) {
            out[j] = ((Number) items[j]).longValue();
        }
        return out;
    }

}
