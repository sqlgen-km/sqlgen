// Code generated by sqlgen; DO NOT EDIT.
package com.example.mapper.arr;

import org.apache.ibatis.session.SqlSession;

/** Factory for creating {@code ArrMapper} instances. */
public class ArrMapperFactory {

    /**
     * Create a {@code ArrMapper} for the given database driver.
     * <p>
     * Usage without Spring:
     * <pre>
     * SqlSession session = sqlSessionFactory.openSession();
     * ArrMapper mapper = ArrMapperFactory.create(session, "postgresql");
     * </pre>
     *
     * Valid driver names: postgresql, mysql, oracle, sqlserver.
     */
    public static ArrMapper create(SqlSession session, String driver) {
        switch (driver) {
            case "postgresql" -> { return session.getMapper(ArrMapperPG.class); }
            case "mysql" -> { return session.getMapper(ArrMapperMySQL.class); }
            case "oracle" -> { return session.getMapper(ArrMapperOracle.class); }
            case "sqlserver" -> { return session.getMapper(ArrMapperMSSQL.class); }
        }
        throw new IllegalArgumentException(
            "Unsupported driver: " + driver + ". Supported: postgresql, mysql, oracle, sqlserver");
    }
}
