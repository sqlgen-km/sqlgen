// Code generated by sqlgen; DO NOT EDIT.
package com.example.mapper.arr;

import com.example.entity.arr.FindGroupNamesByIDs;
import com.example.entity.arr.FindMetadataByNames;
import com.example.entity.arr.InsertServicesTableParams;
import com.example.entity.arr.ServicesTable;

import java.util.List;

import org.apache.ibatis.annotations.Param;
import org.apache.ibatis.annotations.*;
import org.springframework.context.annotation.Profile;

@Mapper
@Profile("oracle")
public interface ArrMapperOracle extends ArrMapper {

    @Override
    @Select("SELECT id, name FROM groups WHERE id MEMBER OF #{group_ids, typeHandler=com.example.mapper.arr.LongArrayTypeHandlerOracle}")
    List<FindGroupNamesByIDs> findGroupNamesByIDs(@Param("group_ids") long[] group_ids);

    @Override
    @Select("SELECT system_id, table_name FROM metadata WHERE table_name MEMBER OF #{table_names, typeHandler=com.example.mapper.arr.StringArrayTypeHandlerOracle}")
    List<FindMetadataByNames> findMetadataByNames(@Param("table_names") String[] table_names);

    @Override
    @Insert("INSERT INTO services_table (id, table_name, operations) VALUES (#{id}, #{tableName}, #{operations, typeHandler=com.example.mapper.arr.StringArrayTypeHandlerOracle})")
    @SelectKey(statement = "SELECT seq_services_table.NEXTVAL FROM dual",
               keyProperty = "id", before = true, resultType = Long.class)
    void insertServicesTable(InsertServicesTableParams params);

    @Override
    @Results({
        @Result(column="operations", property="operations", typeHandler=com.example.mapper.arr.StringArrayTypeHandlerOracle.class)
    })
    @Select("SELECT id, table_name, operations FROM services_table WHERE id = #{id}")
    ServicesTable findServicesTableByID(@Param("id") long id);

}
